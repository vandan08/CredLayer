# ─── Dependencies ───────────────────────────────────────────────
node_modules/

# ─── Build output ───────────────────────────────────────────────
# Solidity / Hardhat
cache/
artifacts/
typechain/
typechain-types/

# Java / Maven
target/

# Next.js
.next/
out/
dist/

# ─── Testing ────────────────────────────────────────────────────
coverage/

# ─── Secrets ────────────────────────────────────────────────────
# Real env files are never committed. The .env.example templates ARE
# committed — they document required configuration without any secrets.
.env
.env.local
.env.*.local

# Local-chain deploy records (ephemeral); real networks are committed
contracts/deployments/hardhat.json
contracts/deployments/localhost.json

# Next.js incremental build cache
*.tsbuildinfo

# Claude Code local tooling
.claude/

# ─── Editors / OS ───────────────────────────────────────────────
.idea/
.vscode/
.DS_Store
*.log
